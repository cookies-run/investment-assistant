version: '3.8'

services:
  app:
    build: .
    container_name: investment-assistant
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8000
      - FRONTEND_DIST=/app/dist
      - TZ=Asia/Shanghai
